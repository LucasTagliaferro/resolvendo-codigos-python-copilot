# 🐍 Desafio Python: Calculando Média de Notas

Este repositório contém a solução para o desafio de calcular a média aritmética de um conjunto de notas fornecidas pelo usuário.

O projeto foi desenvolvido como parte de uma série de exercícios para praticar a lógica de programação em Python, focando em:

* Manipulação de listas (`list`).
* Estruturas de repetição (`while`).
* Tratamento de exceções (`try-except`).
* Cálculos básicos com funções embutidas (`sum()` e `len()`).

## 🚀 Como Executar o Código

Siga os passos abaixo para rodar o script no seu ambiente local:

### Pré-requisitos

Você precisa ter o **Python 3** instalado em sua máquina.

### Execução

1.  **Clone o repositório** para sua máquina local usando o terminal:
    ```bash
    git clone [LINK DO SEU REPOSITÓRIO]
    ```
2.  **Navegue até a pasta** do projeto:
    ```bash
    cd [pasta-do-seu-repositorio]
    ```
3.  **Execute o arquivo** `calculando_media_notas.py`:
    ```bash
    python calculando_media_notas.py
    ```
4.  O programa irá solicitar que você **digite as notas** uma por uma.
5.  Para finalizar a entrada de dados e calcular a média, digite a palavra **`fim`**.

## 💻 Código-fonte

O código responsável pelo cálculo da média está no arquivo `calculando_media_notas.py`.

### Exemplo de Uso